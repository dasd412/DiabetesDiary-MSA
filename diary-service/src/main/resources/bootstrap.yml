spring:
  application:
    name: diary-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://configserver:8071

resilience4j.ratelimiter:
    instances:
      diaryService:
        timeOutDuration: 1000ms
        limitRefreshPeriod: 1000
        limitForPeriod: 1


