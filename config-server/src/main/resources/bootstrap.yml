spring:
  application:
    name: config-server
  profiles:
    active: git #native
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/dasd412/DiabetesDiary-config.git

server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: "*"